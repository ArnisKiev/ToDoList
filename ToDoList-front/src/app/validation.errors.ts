export const VALIDATION_ERRORS = new Map<string, string>()
  .set('required', 'The field can`t be empty')
  .set('email', 'Input correct email')
  .set('confirmPassword', 'The passwords must be the same');
